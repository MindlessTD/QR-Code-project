/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
module.exports = {
    output: 'export', // Enables static export
    trailingSlash: true, // Ensures all paths end with a slash (optional but recommended)
  };
  